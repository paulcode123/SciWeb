---
description: File, Route, and Function Organizational Rules
globs: 
---

All html files go in templates folder, all js/css files go directly into static folder
All GET/POST routes go in [main.py](mdc:main.py) in their corresponding section(first page inits, then everything else, finally the if __name__ == '__main__' section)
For AI functions, all prompts go in [prompts.py](mdc:prompts.py), pydantic models go in [models.py](mdc:models.py), call model in [study.py](mdc:study.py), and use output processor in [output_processor.py](mdc:output_processor.py) if needed.
